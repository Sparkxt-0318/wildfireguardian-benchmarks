{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wildfireguardian.example/schemas/benchmark.schema.json",
  "title": "WildfireGuardian benchmark descriptor",
  "description": "Metadata for a single benchmark case. One benchmark.yaml lives in every benchmark directory alongside README.md, inputs/ and expected/.",
  "type": "object",
  "required": [
    "benchmark_id",
    "label",
    "title",
    "category",
    "difficulty",
    "purpose",
    "solver",
    "assumptions",
    "expected_behavior",
    "exactness",
    "detects"
  ],
  "additionalProperties": false,
  "properties": {
    "benchmark_id": {
      "type": "string",
      "pattern": "^WG-BM-[0-9]{3}$",
      "description": "Stable identifier. Never reused, never renumbered."
    },
    "label": {
      "type": "string",
      "pattern": "^[A-J][0-9]+$",
      "description": "Family label from the design document, e.g. F5."
    },
    "title": {"type": "string", "minLength": 3},
    "category": {
      "type": "string",
      "enum": [
        "terrain",
        "road_graph",
        "fire",
        "observation",
        "routing",
        "assisted_dispatch",
        "traffic",
        "forecast_value",
        "scenario_uncertainty",
        "statistics",
        "protectability"
      ]
    },
    "difficulty": {
      "type": "string",
      "enum": ["basic", "intermediate", "adversarial"]
    },
    "purpose": {"type": "string", "minLength": 10},
    "solver": {
      "type": "string",
      "description": "Identifier of the reference solver that turns inputs/ into a result document.",
      "minLength": 3
    },
    "inputs": {
      "type": "object",
      "description": "Named input documents, given as paths relative to the benchmark directory.",
      "additionalProperties": {"type": "string"}
    },
    "assumptions": {
      "type": "object",
      "description": "Semantic switches the expected answer depends on. A benchmark whose answer changes under a different convention MUST state the convention here.",
      "additionalProperties": {
        "type": ["boolean", "string", "number", "null"]
      }
    },
    "expected_behavior": {
      "type": "object",
      "description": "Headline expected values, duplicated from expected/expected.yaml for readability. Consistency is enforced by tests.",
      "minProperties": 1
    },
    "tolerance": {
      "type": "object",
      "description": "Numeric tolerances. 'default' applies everywhere; other keys override by leaf name or dotted result path. A scalar is an absolute tolerance; {abs: .., rel: ..} sets both.",
      "additionalProperties": {
        "anyOf": [
          {"type": "number", "minimum": 0},
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "abs": {"type": "number", "minimum": 0},
              "rel": {"type": "number", "minimum": 0}
            }
          }
        ]
      }
    },
    "exactness": {
      "type": "string",
      "enum": ["exact_analytic", "exact_enumeration", "seeded_stochastic", "qualitative"],
      "description": "How the expected answer was obtained. exact_analytic: closed form derived by hand. exact_enumeration: finite exhaustive enumeration. seeded_stochastic: deterministic given the seed, with a stated tolerance. qualitative: an ordering or sign, not a number."
    },
    "detects": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "pattern": "^[a-z0-9_]+$"},
      "description": "Failure-mode identifiers this benchmark is designed to catch."
    },
    "mutations_expected_to_fail": {
      "type": "array",
      "items": {"type": "string", "pattern": "^[a-z0-9_]+$"},
      "description": "Mutation ids from wg_benchmarks.mutations that this benchmark must detect."
    },
    "hand_checkable": {
      "type": "boolean",
      "description": "True when a competent reviewer can verify the expected answer with pen and paper in a few minutes."
    },
    "figures": {
      "type": "array",
      "items": {"type": "string"}
    },
    "notes": {"type": "string"},
    "references": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
