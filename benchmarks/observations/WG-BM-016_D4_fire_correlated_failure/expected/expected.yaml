# GENERATED by tools/authoring/author_observation.py. Edit the script, not this file.
benchmark_id: WG-BM-016
source: hand_derivation
derivation: "Fire reaches s1 at 20, s2 at 30, s3 at 40, s4 at 90 and s5 at 120 minutes, and each sensor is destroyed on arrival. At t = 60 the silent sensors are s1, s2 and s3 and the reporting ones are s4 and s5. Every reporting sensor reports 0 (unburnt), so the mean over reporting sensors is 0.0. The true fraction burnt is 3/5 = 0.6, so the naive estimate is biased by -0.6: it is the maximum possible error, and it is achieved with data that contain no contradictory value at all. An estimator that treats 'silent after hazard arrival' as evidence of burning recovers (3 * 1 + 2 * 0)/5 = 0.6 exactly. Individually, s1's last record is at 15 minutes (the last 5-minute slot before its hazard arrival at 20), so at t = 60 it is 45 minutes stale and its current value is unknown."
results:
  mnar:
    at_time_min: 60.0
    reporting_sensors: [s4, s5]
    silent_sensors: [s1, s2, s3]
    observed_mean: 0.0
    true_mean: 0.6
    naive_bias: -0.6
    dropout_aware_mean: 0.6
    dropout_aware_bias: 0.0
    missingness_informative: true
  queries:
    q_s1_at_60:
      latest_acquisition_time_min: 15.0
      value: null
      staleness_min: 45.0
      stale: true
      status: stale
    q_s5_at_60:
      latest_acquisition_time_min: 60.0
      value: 0.0
      staleness_min: 0.0
      stale: false
      status: current
invariants:
  - expression: "r['mnar']['observed_mean'] < r['mnar']['true_mean']"
    description: the complete-case estimate understates the hazard
  - expression: "abs(r['mnar']['dropout_aware_bias']) < 1e-12"
    description: treating dropout as evidence recovers the truth exactly here
