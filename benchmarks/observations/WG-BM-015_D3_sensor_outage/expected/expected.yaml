# GENERATED by tools/authoring/author_observation.py. Edit the script, not this file.
benchmark_id: WG-BM-015
source: hand_derivation
derivation: "Latency is zero and the declared freshness requirement is 5 minutes. At t = 15 the sensor is still reporting: the latest acquisition is 15, staleness 0, value truth(15) = 700 m, current. At t = 30 the sensor has been silent since 15, so the latest acquisition is still 15 and staleness is 30 - 15 = 15 min, which exceeds the 5-minute requirement. The reported value is therefore null with status 'stale'; the last known value, 700 m, is retained separately so that a caller who explicitly wants it must ask for it. The contemporaneous truth is truth(30) = 400 m, so the stale value is 300 m optimistic. At t = 50 the sensor has resumed: latest acquisition 50, staleness 0, value truth(50) = 0 m, current."
results:
  queries:
    q15:
      latest_acquisition_time_min: 15.0
      value: 700.0
      staleness_min: 0.0
      stale: false
      status: current
    q30:
      latest_acquisition_time_min: 15.0
      value: null
      last_known_value: 700.0
      staleness_min: 15.0
      stale: true
      status: stale
      truth_value: 400.0
      matches_truth: false
    q50:
      latest_acquisition_time_min: 50.0
      value: 0.0
      staleness_min: 0.0
      stale: false
      status: current
invariants:
  - expression: "r['queries']['q30']['value'] is None and r['queries']['q30']['last_known_value'] == 700.0"
    description: during the outage the current value is unknown but the last known value is retained
  - expression: "r['queries']['q30']['value'] != 0.0"
    description: missing is not zero
