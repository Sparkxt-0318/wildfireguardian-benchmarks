# GENERATED by tools/authoring/author_observation.py. Edit the script, not this file.
benchmark_id: WG-BM-017
source: exhaustive_enumeration
derivation: "Under the rule 'no detection means no fire', both cells look clear, so route_south is chosen for its believed loss of 0 against route_north's 5. In truth c_south is on fire, so the realised loss of route_south is 100. Under the rule 'a non-detection is informative only where the detector is reliable', c_north is clear (loss 5) and c_south is unknown, hence treated as possibly hazardous (believed loss 100); route_north is chosen and its realised loss is 5, because c_north really is clear. The regret of trusting the unreliable non-detection is 100 - 5 = 95."
results:
  detection:
    true_fire_cells: [c_south]
    detected_cells: []
    false_negative_cells: [c_south]
    reliably_clear_cells: [c_north]
    false_negative_present: true
    route_trusting_detections: route_south
    loss_trusting_detections: 100.0
    route_precautionary: route_north
    loss_precautionary: 5.0
    regret_of_trusting: 95.0
invariants:
  - expression: "r['detection']['loss_trusting_detections'] > r['detection']['loss_precautionary']"
    description: trusting the unreliable non-detection is strictly worse
  - expression: "r['detection']['detected_cells'] == []"
    description: "no detection anywhere: the difference comes entirely from reliability"
