# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-022
source: hand_derivation
derivation: "Let d be the dispatch time. The responder reaches the resident at d + 5 and the pickup finishes at d + 10. Three constraints apply. (1) Ingress: the interval [d, d + 5] must fit in [0, 30], giving d <= 25. (2) Resident tenability: the pickup must finish by minute 25, giving d + 10 <= 25, i.e. d <= 15. (3) Egress: departure is at d + 10 and arrival at d + 15, and the interval [d + 10, d + 15] must fit in [0, 20], giving d + 15 <= 20, i.e. d <= 5. The binding constraint is the egress closure, so the latest feasible dispatch is d = 5 and the feasible set is the single interval [0, 5]. At d = 0 the refuge is reached at 15; at d = 5 it is reached at exactly 20, with zero slack; at d = 6 the egress traversal would run to 21 and the mission fails at the egress stage."
results:
  resident: resident
  resident_deadline_min: 25.0
  feasible_intervals:
    - [0.0, 5.0]
  feasible_interval_count: 1
  latest_feasible_dispatch_min: 5.0
  earliest_feasible_dispatch_min: 0.0
  monotone_feasibility: true
  scalar_latest_sufficient: true
  infeasible_dispatch_below_latest_min: null
  best_base: base
  latest_dispatch_by_base:
    base: 5.0
  probes:
    p00:
      success: true
      base: base
      destination: refuge
      arrival_min: 15.0
      feasible_bases: [base]
    p05:
      success: true
      base: base
      destination: refuge
      arrival_min: 20.0
    p06:
      success: false
      failure_stage: egress
      arrival_min: null
invariants:
  - expression: "r['feasible_intervals'][0][0] == 0.0"
    description: dispatching immediately is feasible
  - expression: "abs(r['latest_feasible_dispatch_min'] - 5.0) < 1e-6"
    description: the latest dispatch time is exactly 5 minutes
