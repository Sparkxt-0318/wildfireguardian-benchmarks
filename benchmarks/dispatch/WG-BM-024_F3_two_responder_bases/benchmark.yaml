# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-024
label: F3
title: The nearest base is not the right base when its corridor closes first
category: assisted_dispatch
difficulty: intermediate
purpose: "Base selection must consider the ingress corridor's closure time, not just travel distance. The far base gives 5 more minutes of dispatch latitude."
solver: dispatch.mission_enumeration
assumptions:
  edge_semantics: WG-SEM-1 interval safety
  waiting: false
  pickup_min: 5
  one_vehicle_per_base: true
expected_behavior:
  latest_dispatch_by_base:
    base_near: 2.0
    base_far: 7.0
  best_base: base_far
  nearest_base: base_near
tolerance:
  default: 1.0e-06
exactness: CLOSED_FORM
detects: [greedy_base_selection, ingress_corridor_ignored]
mutations_expected_to_fail: [nearest_base_only]
hand_checkable: true
inputs:
  network: inputs/network.yaml
  mission: inputs/mission.yaml
