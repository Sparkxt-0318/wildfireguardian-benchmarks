# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-024
source: hand_derivation
derivation: "From the near base the ingress interval is [d, d + 4], which must fit inside [0, 6], so d <= 2. Its egress constraint, [d + 9, d + 17] inside [0, 30], gives d <= 13 and does not bind. The near base therefore offers latest dispatch 2. From the far base the ingress interval [d, d + 10] inside [0, 60] gives d <= 50, and the egress interval [d + 15, d + 23] inside [0, 30] gives d <= 7. The far base therefore offers latest dispatch 7, five minutes more than the near base, despite a 6 minute longer drive. Overall the feasible dispatch set is [0, 7]. At d = 0 both bases work and the near one arrives sooner (17 versus 23), so it is selected. At d = 5 only the far base works, arriving at 28. At d = 8 neither works."
results:
  bases_considered: [base_near, base_far]
  nearest_base: base_near
  latest_dispatch_by_base:
    base_near: 2.0
    base_far: 7.0
  best_base: base_far
  feasible_intervals:
    - [0.0, 7.0]
  latest_feasible_dispatch_min: 7.0
  monotone_feasibility: true
  probes:
    p00:
      success: true
      base: base_near
      arrival_min: 17.0
      feasible_bases: [base_far, base_near]
    p05:
      success: true
      base: base_far
      arrival_min: 28.0
      feasible_bases: [base_far]
    p08:
      success: false
      feasible_bases: []
invariants:
  - expression: "r['best_base'] != r['nearest_base']"
    description: the base offering the most dispatch latitude is not the nearest one
  - expression: "r['probes']['p05']['feasible_bases'] == ['base_far']"
    description: at minute 5 only the far base can still complete the mission
