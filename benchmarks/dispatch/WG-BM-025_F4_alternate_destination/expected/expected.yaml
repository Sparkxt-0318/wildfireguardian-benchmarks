# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-025
source: hand_derivation
derivation: "Pickup finishes at d + 10 in every case. To the near refuge: the egress interval [d + 10, d + 15] must fit in [0, 18], so d <= 3, and the arrival is d + 15. To the far shelter: [d + 10, d + 30] inside [0, 100] gives d <= 70, and the arrival is d + 30. The resident's location is untenable after minute 25, so the pickup constraint d + 10 <= 25 gives d <= 15, which is what limits the shelter option. Overall the mission is feasible exactly for d in [0, 15]. At d = 0 both destinations work and the refuge arrives first (15 versus 30), so it is chosen. At d = 6 the refuge is unreachable (its approach would be occupied to minute 21) and the shelter is chosen, arriving at 36. At d = 15 only the shelter remains, arriving at 45 with the pickup finishing exactly on the tenability deadline. At d = 20 the pickup would finish at 30, five minutes after the resident's location is untenable, and the mission fails at the pickup stage."
results:
  resident_deadline_min: 25.0
  feasible_intervals:
    - [0.0, 15.0]
  latest_feasible_dispatch_min: 15.0
  monotone_feasibility: true
  probes:
    p00:
      success: true
      destination: refuge_close
      arrival_min: 15.0
    p06:
      success: true
      destination: shelter_far
      arrival_min: 36.0
    p15:
      success: true
      destination: shelter_far
      arrival_min: 45.0
    p20:
      success: false
      failure_stage: pickup
invariants:
  - expression: "r['probes']['p00']['destination'] != r['probes']['p06']['destination']"
    description: the correct destination changes with the dispatch time
  - expression: "r['probes']['p20']['failure_stage'] == 'pickup'"
    description: "late dispatch fails because the resident's location is untenable, not because of the roads"
