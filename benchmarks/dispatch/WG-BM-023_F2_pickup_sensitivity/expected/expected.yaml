# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-023
source: hand_derivation
derivation: "With pickup duration p the refuge is reached at d + 5 + p + 5 = d + p + 10, and the binding egress constraint is d + p + 10 <= 20, i.e. d <= 10 - p. p = 2 gives d <= 8; p = 5 gives d <= 5; p = 10 gives d <= 0, so only an instantaneous dispatch works; p = 15 gives d <= -5, which is impossible, so the feasible set is empty and the mission cannot be completed at any dispatch time. The resident tenability constraint d + p <= 15 is slacker than the egress constraint for every p in this sweep, so it never binds. The latest dispatch therefore falls one minute for every extra minute of on-scene time, a slope of exactly -1."
results:
  pickup_values_min: [2.0, 5.0, 10.0, 15.0]
  latest_dispatch_by_pickup:
    "2": 8.0
    "5": 5.0
    "10": 0.0
    "15": null
  feasible_by_pickup:
    "2": true
    "5": true
    "10": true
    "15": false
  latest_feasible_dispatch_min: 8.0
  probes:
    p_short_pickup:
      success: true
      destination: refuge
      arrival_min: 20.0
    p_long_pickup:
      success: false
      failure_stage: egress
invariants:
  - expression: "r['latest_dispatch_by_pickup']['2'] - r['latest_dispatch_by_pickup']['5'] == 3.0"
    description: "3 extra minutes of pickup cost exactly 3 minutes of dispatch latitude"
  - expression: "r['latest_dispatch_by_pickup']['5'] - r['latest_dispatch_by_pickup']['10'] == 5.0"
    description: "5 extra minutes of pickup cost exactly 5 minutes of dispatch latitude"
  - expression: "r['feasible_by_pickup']['15'] is False"
    description: a 15 minute pickup makes the mission impossible at any dispatch time
