# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-026
source: exhaustive_enumeration
derivation: "The ingress traversal takes 6 minutes and must fit inside one open window of the corridor. Inside [0, 10] that requires d + 6 <= 10, i.e. d <= 4. Inside [20, 40] it requires d >= 20 and d + 6 <= 40, i.e. 20 <= d <= 34. No departure in (4, 20) works: the traversal would either still be in progress when the front arrives or would start before the road reopens. The egress leg departs at d + 10 and arrives at d + 20, and [d + 10, d + 20] inside [0, 60] gives d <= 40, which does not bind. The resident's location is tenable to minute 90, which also does not bind. The feasible dispatch set is therefore the union of [0, 4] and [20, 34]: two disjoint intervals. The latest feasible dispatch is 34, yet dispatching at any time in (4, 20) - for example at 5, or at 10 - fails. A scalar deadline is necessarily wrong about that entire 16 minute gap."
results:
  feasible_intervals:
    - [0.0, 4.0]
    - [20.0, 34.0]
  feasible_interval_count: 2
  latest_feasible_dispatch_min: 34.0
  earliest_feasible_dispatch_min: 0.0
  monotone_feasibility: false
  scalar_latest_sufficient: false
  infeasible_dispatch_below_latest_min: 5.0
  probes:
    p02:
      success: true
      arrival_min: 22.0
    p10:
      success: false
      failure_stage: ingress
    p25:
      success: true
      arrival_min: 45.0
    p36:
      success: false
      failure_stage: ingress
invariants:
  - expression: "len(r['feasible_intervals']) == 2"
    description: the feasible dispatch set is genuinely two intervals
  - expression: "r['infeasible_dispatch_below_latest_min'] < r['latest_feasible_dispatch_min']"
    description: there exists a dispatch time below the latest feasible time that fails
  - expression: "r['probes']['p10']['success'] is False and r['probes']['p25']['success'] is True"
    description: "feasible, infeasible, feasible as the dispatch time increases"
