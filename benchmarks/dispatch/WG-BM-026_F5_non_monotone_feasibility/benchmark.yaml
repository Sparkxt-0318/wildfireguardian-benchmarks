# GENERATED by tools/authoring/author_dispatch.py. Edit the script, not this file.
benchmark_id: WG-BM-026
label: F5
title: "Dispatch feasibility is feasible, then infeasible, then feasible again"
category: assisted_dispatch
difficulty: adversarial
purpose: Prove that a single scalar latest-dispatch-time is not sufficient. The feasible set here is two disjoint intervals and dispatching at minute 10 - comfortably below the latest feasible time of 34 - fails.
solver: dispatch.mission_enumeration
assumptions:
  edge_semantics: WG-SEM-1 interval safety
  waiting: false
  pickup_min: 4
  corridor_reopens: true
expected_behavior:
  feasible_intervals:
    - [0.0, 4.0]
    - [20.0, 34.0]
  monotone_feasibility: false
  scalar_latest_sufficient: false
  infeasible_dispatch_below_latest_min: 5.0
tolerance:
  default: 1.0e-06
exactness: exact_enumeration
detects: [non_monotone_feasibility, scalar_deadline_insufficient]
mutations_expected_to_fail: [monotone_dispatch_assumption]
hand_checkable: true
notes: "This is the single most important benchmark in the F family. If a system can only express one number, it cannot express this scenario correctly."
inputs:
  network: inputs/network.yaml
  mission: inputs/mission.yaml
figures: [figures/feasible_dispatch_intervals.svg]
