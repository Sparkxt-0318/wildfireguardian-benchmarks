# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
benchmark_id: WG-BM-028
source: hand_derivation
derivation: "Both forecasts place the front on the correct side of the 2 km decision boundary in both scenarios, so both produce the same signal map and therefore the same actions: take the valley on 'far' (loss 1 beats 8) and the ridge on 'near' (loss 8 beats 100). Each forecast-aware policy has expected loss 0.9 * 1 + 0.1 * 8 = 1.7 against the baseline's 8, so each is worth 8 - 1.7 = 6.3. The clairvoyant expected loss is also 1.7, so EVPI = 6.3 and both forecasts capture all of it. The exact forecast scores 1.0 on skill and the displaced one 0.6, yet their decision value is identical to the last decimal: the ranking by skill and the ranking by value disagree. In the realised scenario s_far both forecast policies lose 1 against the baseline's 8."
results:
  truth_scenario: s_far
  baseline_policy: baseline_ridge
  expected_loss_by_policy:
    baseline_ridge: 8.0
    forecast_aware_exact: 1.7
    forecast_aware_displaced: 1.7
  value_by_policy:
    baseline_ridge: 0.0
    forecast_aware_exact: 6.3
    forecast_aware_displaced: 6.3
  realised_loss_by_policy:
    baseline_ridge: 8.0
    forecast_aware_exact: 1.0
    forecast_aware_displaced: 1.0
  clairvoyant_expected_loss: 1.7
  best_fixed_action: route_ridge
  best_fixed_expected_loss: 8.0
  evpi: 6.3
  realizable_value_of_information: 6.3
  timely_information_sources: [forecast_displaced, forecast_exact]
  late_information_sources: []
  ranked_by_skill: [forecast_aware_exact, forecast_aware_displaced, baseline_ridge]
  skill_and_value_agree: false
invariants:
  - expression: "abs(r['value_by_policy']['forecast_aware_exact'] - r['value_by_policy']['forecast_aware_displaced']) < 1e-12"
    description: a 500 m degradation in accuracy changes the decision value by exactly zero
  - expression: "r['policies']['forecast_aware_exact']['action_by_scenario'] == r['policies']['forecast_aware_displaced']['action_by_scenario']"
    description: both forecasts drive identical actions in every scenario
