# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
benchmark_id: WG-BM-033
source: hand_derivation
derivation: "The forecast says 'benign' in all five scenarios, so the forecast-aware policy waits in all five. Its expected loss is 0.8 * 0 + 0.2 * 200 = 40 against the robust baseline's 10, so the value of acting on the forecast is 10 - 40 = -30: Delta J is negative and the forecast is harmful. This happens although the forecast is correct in four cases out of five - a classification accuracy of 0.8 - because its single error falls in the only scenario whose consequences are large. In the realised scenario s5 the forecast-aware policy loses 200 against the baseline's 10, a realised value of -190 and, since evacuating early is the best action there, a realised regret of 190. The clairvoyant expected loss is 0.8 * 0 + 0.2 * 10 = 2, so EVPI is 10 - 2 = 8: a forecast that caught the extreme scenario would have been worth 8, and this one is worth -30."
results:
  truth_scenario: s5
  expected_loss_by_policy:
    robust_baseline: 10.0
    forecast_aware: 40.0
  value_by_policy:
    robust_baseline: 0.0
    forecast_aware: -30.0
  realised_loss_by_policy:
    robust_baseline: 10.0
    forecast_aware: 200.0
  realised_value_by_policy:
    robust_baseline: 0.0
    forecast_aware: -190.0
  realised_regret_by_policy:
    robust_baseline: 0.0
    forecast_aware: 190.0
  clairvoyant_expected_loss: 2.0
  best_fixed_action: evacuate_early
  best_fixed_expected_loss: 10.0
  evpi: 8.0
  realizable_value_of_information: -30.0
  recommended_policy: robust_baseline
  ranked_by_value: [robust_baseline, forecast_aware]
  ranked_by_skill: [forecast_aware, robust_baseline]
  skill_and_value_agree: false
invariants:
  - expression: "r['value_by_policy']['forecast_aware'] < 0"
    description: "Delta J is negative: the forecast-aware policy is worse than the baseline"
  - expression: "r['evpi'] > 0 > r['realizable_value_of_information']"
    description: "information would help; this particular forecast harms"
