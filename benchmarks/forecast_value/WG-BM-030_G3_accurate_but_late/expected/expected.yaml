# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
benchmark_id: WG-BM-030
source: hand_derivation
derivation: "The forecast becomes available at minute 30 and the decision must be taken by minute 20, so it cannot influence the action at all. A policy that waits for it has, by minute 20, taken no evacuation decision, which is the shelter-in-place outcome; its expected loss is 0.5 * 100 + 0.5 * 0 = 50 against the baseline trigger's 10, so its value is 10 - 50 = -40. In the realised scenario s_hit it loses 100 against the baseline's 10, a realised value of -90 and a realised regret of 90. The information itself is genuinely valuable: the clairvoyant expected loss is 0.5 * 10 + 0.5 * 0 = 5 against the best fixed action's 10, so EVPI = 5. None of it is realisable here, because no timely policy can use it: realizable_value_of_information = 0. High skill, positive EVPI, zero realisable value and negative realised value, all at once."
results:
  truth_scenario: s_hit
  expected_loss_by_policy:
    baseline_trigger: 10.0
    wait_for_forecast: 50.0
  value_by_policy:
    baseline_trigger: 0.0
    wait_for_forecast: -40.0
  realised_loss_by_policy:
    baseline_trigger: 10.0
    wait_for_forecast: 100.0
  realised_value_by_policy:
    baseline_trigger: 0.0
    wait_for_forecast: -90.0
  realised_regret_by_policy:
    baseline_trigger: 0.0
    wait_for_forecast: 90.0
  timely_information_sources: []
  late_information_sources: [forecast_accurate_late]
  clairvoyant_expected_loss: 5.0
  best_fixed_action: evacuate
  best_fixed_expected_loss: 10.0
  evpi: 5.0
  realizable_value_of_information: 0.0
  recommended_policy: baseline_trigger
invariants:
  - expression: "r['evpi'] > 0 and r['realizable_value_of_information'] == 0.0"
    description: the information is valuable in principle and worthless in practice
  - expression: "r['policies']['wait_for_forecast']['information_timely'] is False"
    description: the forecast is explicitly marked untimely
