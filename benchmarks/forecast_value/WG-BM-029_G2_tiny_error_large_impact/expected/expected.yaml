# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
benchmark_id: WG-BM-029
source: hand_derivation
derivation: "The forecast is 20 m in error and that error lands on the wrong side of the 1000 m decision boundary, so it reports 'clear' in both scenarios and the forecast-aware policy takes the valley road in both. Its expected loss is 0.5 * 1 + 0.5 * 100 = 50.5 against the baseline's 8, so the forecast is worth 8 - 50.5 = -42.5: strongly negative. In the realised scenario s_near the valley road costs 100 while the best available action, the ridge road, costs 8, so the realised regret is 92. For comparison the clairvoyant expected loss is 0.5 * 1 + 0.5 * 8 = 4.5 and EVPI is 8 - 4.5 = 3.5, so a forecast that resolved this boundary correctly would have been worth 3.5; the one that missed it by 20 m is worth -42.5."
results:
  truth_scenario: s_near
  expected_loss_by_policy:
    baseline_ridge: 8.0
    forecast_aware: 50.5
  value_by_policy:
    baseline_ridge: 0.0
    forecast_aware: -42.5
  realised_loss_by_policy:
    baseline_ridge: 8.0
    forecast_aware: 100.0
  realised_regret_by_policy:
    baseline_ridge: 0.0
    forecast_aware: 92.0
  best_loss_in_truth_scenario: 8.0
  clairvoyant_expected_loss: 4.5
  best_fixed_action: route_ridge
  evpi: 3.5
  realizable_value_of_information: -42.5
  recommended_policy: baseline_ridge
  skill_and_value_agree: false
  action_in_truth_by_policy:
    baseline_ridge: route_ridge
    forecast_aware: route_valley
invariants:
  - expression: "r['value_by_policy']['forecast_aware'] < 0"
    description: a 0.98-skill forecast has negative decision value here
  - expression: "r['realised_regret_by_policy']['forecast_aware'] == 92.0"
    description: a 20 m error produces a realised regret of 92
