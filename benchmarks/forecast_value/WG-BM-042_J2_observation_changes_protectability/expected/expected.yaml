# GENERATED by tools/authoring/author_protectability.py. Edit the script, not this file.
benchmark_id: WG-BM-042
source: hand_derivation
derivation: "Expected losses of the fixed actions: evacuate_north 0.5*0 + 0.5*90 = 45, evacuate_south 0.5*95 + 0.5*0 = 47.5, shelter 50. The best fixed action is evacuate_north at 45, and its worst case is 90, far outside the acceptable threshold of 10: no action is acceptable across both worlds, so the resident is not robustly protectable on the prior alone. The clairvoyant expected loss is 0.5*0 + 0.5*0 = 0, so EVPI = 45 - 0 = 45. The recon flight arrives at minute 5, before the minute-10 deadline, and distinguishes the worlds, so the informed policy evacuates north on 'north' and south on 'south', with expected loss 0. Its value is the full 45. In the realised world w_south the prior-based policy loses 90 and the informed policy loses 0."
results:
  truth_scenario: w_south
  expected_loss_by_policy:
    act_on_prior: 45.0
    wait_for_recon: 0.0
  value_by_policy:
    act_on_prior: 0.0
    wait_for_recon: 45.0
  realised_loss_by_policy:
    act_on_prior: 90.0
    wait_for_recon: 0.0
  realised_value_by_policy:
    act_on_prior: 0.0
    wait_for_recon: 90.0
  clairvoyant_expected_loss: 0.0
  best_fixed_action: evacuate_north
  best_fixed_expected_loss: 45.0
  evpi: 45.0
  realizable_value_of_information: 45.0
  timely_information_sources: [recon_flight]
  late_information_sources: []
  identical_actions_to_baseline:
    act_on_prior: true
    wait_for_recon: false
  policies:
    act_on_prior:
      worst_case_loss: 90.0
    wait_for_recon:
      worst_case_loss: 0.0
invariants:
  - expression: "r['policies']['act_on_prior']['worst_case_loss'] > 10.0"
    description: no fixed action is acceptable in both worlds
  - expression: "r['policies']['wait_for_recon']['worst_case_loss'] <= 10.0"
    description: "with the observation, the resident becomes protectable in every world"
  - expression: "abs(r['realizable_value_of_information'] - r['evpi']) < 1e-12"
    description: the timely observation captures the entire value of information
