# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
benchmark_id: WG-BM-032
source: hand_derivation
derivation: "Both information sources separate s_hit from s_miss and both are available before the deadline, so both drive the same actions: evacuate when the fire is coming (5 beats 100) and stay when it is not (0 beats 5). Each policy therefore has expected loss 0.5 * 5 + 0.5 * 0 = 2.5, and the added value of the sophisticated forecast over the trip wire is exactly 0. Measured instead against the best fixed action, which is to evacuate always at expected loss 5, the value of perfect information is 5 - 2.5 = 2.5. So the information in this problem is worth 2.5 and the trip wire has already collected all of it. In the realised scenario s_hit both policies evacuate and both lose 5."
results:
  truth_scenario: s_hit
  baseline_policy: trigger_policy
  expected_loss_by_policy:
    trigger_policy: 2.5
    forecast_policy: 2.5
  value_by_policy:
    trigger_policy: 0.0
    forecast_policy: 0.0
  realised_loss_by_policy:
    trigger_policy: 5.0
    forecast_policy: 5.0
  identical_actions_to_baseline:
    trigger_policy: true
    forecast_policy: true
  clairvoyant_expected_loss: 2.5
  best_fixed_action: evacuate
  best_fixed_expected_loss: 5.0
  evpi: 2.5
  realizable_value_of_information: 0.0
  timely_information_sources: [forecast_model, trigger_3km]
invariants:
  - expression: "r['value_by_policy']['forecast_policy'] == 0.0"
    description: the forecast adds exactly nothing to the trip-wire baseline
  - expression: "r['evpi'] > 0"
    description: "information is valuable in this problem; the baseline already has it"
