# GENERATED by tools/authoring/author_forecast_value.py. Edit the script, not this file.
description: "A fire-within-3-km trip wire is available from minute 0 and a model forecast from minute 5. Both separate the same two scenarios, so both drive the same actions."
scenarios:
  - id: s_hit
    probability: 0.5
  - id: s_miss
    probability: 0.5
actions: [evacuate, stay]
loss:
  evacuate:
    s_hit: 5.0
    s_miss: 5.0
  stay:
    s_hit: 100.0
    s_miss: 0.0
decision_deadline_min: 20.0
default_action: evacuate
truth_scenario: s_hit
information_sources:
  - id: trigger_3km
    available_at_min: 0.0
    signal:
      s_hit: tripped
      s_miss: clear
    skill_score: 0.5
    spatial_error_m: 3000.0
  - id: forecast_model
    available_at_min: 5.0
    signal:
      s_hit: hit
      s_miss: miss
    skill_score: 0.95
    spatial_error_m: 50.0
policies:
  - id: trigger_policy
    type: informed
    source: trigger_3km
  - id: forecast_policy
    type: informed
    source: forecast_model
baseline_policy: trigger_policy
