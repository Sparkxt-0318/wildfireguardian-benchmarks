# GENERATED by tools/authoring/author_probabilistic_forecast.py. Edit the script, not this file.
benchmark_id: WG-BM-044
source: closed_form
derivation: "The decision threshold follows from the loss matrix alone. Writing p for the probability that the front is inside 800 m, the expected loss of using the road is 100p and of the long way is 8, so the actions are indifferent at p* = 8/100 = 0.08 and the road is used only below that. Forecast A is N(1000, 50^2), so p_A = Phi((800 - 1000)/50) = Phi(-4) = 3.167124183311998e-05, which is far below 0.08: use the road. Forecast B is N(1000, 300^2), so p_B = Phi((800 - 1000)/300) = Phi(-2/3) = 0.2524925375469229, which is above 0.08: take the long way. Both forecasts have mean 1000 m, so a policy that reads only the point estimate sees 1000 > 800 in both cases, calls the road clear in both cases, and cannot distinguish them. The distinction is entirely in the spread."
results:
  model: predictive_threshold
  threshold: 800.0
  decision_threshold_probability: 0.08
  action_partition:
    - p_from: 0.0
      p_to: 0.08
      action: use_road
    - p_from: 0.08
      p_to: 1.0
      action: long_way
  hazard_probability_by_forecast:
    forecast_a: 3.167124183311998e-05
    forecast_b: 0.2524925375469229
  action_by_forecast:
    forecast_a: use_road
    forecast_b: long_way
  point_estimate_action_by_forecast:
    forecast_a: use_road
    forecast_b: use_road
  point_estimate_distinguishes_forecasts: false
  distribution_distinguishes_forecasts: true
  forecasts:
    forecast_a:
      mean: 1000.0
      sd: 50.0
      expected_loss:
        use_road: 0.003167124183311998
        long_way: 8.0
    forecast_b:
      mean: 1000.0
      sd: 300.0
      expected_loss:
        use_road: 25.24925375469229
        long_way: 8.0
invariants:
  - expression: "r['forecasts']['forecast_a']['mean'] == r['forecasts']['forecast_b']['mean']"
    description: the two forecasts have exactly the same point prediction
  - expression: "r['action_by_forecast']['forecast_a'] != r['action_by_forecast']['forecast_b']"
    description: and nevertheless require different actions
  - expression: "len(set(r['point_estimate_action_by_forecast'].values())) == 1"
    description: a point-estimate policy cannot tell them apart
