# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-054
source: closed_form
derivation: "Each sensor alone reports 1 with probability 0.8 given danger and 0.2 given safety: the declared joint reproduces exactly those marginals, since 0.76 + 0.04 = 0.80 and 0.16 + 0.04 = 0.20. What differs is the dependence - the two agree with probability 0.92 rather than the 0.68 that independence implies. With a prior of 0.5 and both sensors reporting clear, P(dangerous | 0,0) = 0.16 / (0.16 + 0.76) = 4/23 = 0.17391304347826086. Multiplying the marginals instead gives 0.04 and 0.64, hence 0.04 / 0.68 = 1/17 = 0.058823529411764705 - three times more confident that the route is safe. In log terms the true evidence is log2(0.16/0.76) = -2.2479275134435857 bits and the independence assumption claims exactly -4 bits, because it counts two full sensors' worth of evidence from two sensors that largely repeat each other. The loss matrix gives p* = 10 / (10 + 90) = 0.1. The true posterior 0.174 is above it, so the correct action is to divert; the overconfident 0.059 is below it, so a system assuming independence proceeds. Note that under the correct joint the observation never changes the action - every branch diverts, so EVSI is exactly 0. The independence assumption does not merely add noise: it manufactures a decision change out of an observation that should not have produced one."
results:
  model: bayes_decision
  prior_hazard: 0.5
  prior_action: divert
  prior_expected_loss: 5.0
  decision_threshold_probability: 0.1
  evpi: 5.0
  observations:
    sensor_pair:
      evsi_statistical: 0.0
      information_gain_bits: 0.3067479106446732
      changes_action: false
      realised_outcome: "0,0"
      realised_posterior_hazard: 0.17391304347826086
      realised_action: divert
      realised_posterior_under_independence: 0.058823529411764705
      realised_action_under_independence: proceed
      realised_log_likelihood_ratio_bits: -2.2479275134435857
      realised_log_likelihood_ratio_bits_under_independence: -4.0
invariants:
  - expression: "r['observations']['sensor_pair']['realised_posterior_hazard'] > r['decision_threshold_probability']"
    description: the correct posterior is above the threshold
  - expression: "r['observations']['sensor_pair']['realised_posterior_under_independence'] < r['decision_threshold_probability']"
    description: and the independence-assumed posterior is below it
  - expression: "r['observations']['sensor_pair']['realised_log_likelihood_ratio_bits_under_independence'] < r['observations']['sensor_pair']['realised_log_likelihood_ratio_bits']"
    description: independence claims strictly more evidence than the data contain
