# GENERATED by tools/authoring/author_probabilistic_forecast.py. Edit the script, not this file.
benchmark_id: WG-BM-047
source: closed_form
derivation: "Here the conservative action fully protects, so E[proceed] = p * L_failure and E[hold_back] = (1 - p) * L_conservative. Indifference gives p* L_f = (1 - p*) L_c, hence p* = L_c / (L_c + L_f) = 5 / (5 + 495) = 0.01. At p = 0.005: E[proceed] = 2.475 against E[hold_back] = 4.975, so proceed. At p = 0.02: E[proceed] = 9.9 against 4.9, so hold back. At p = 0.4: E[proceed] = 198 against 3.0, so hold back - and a half-probability rule would proceed, at 66 times the optimal expected loss. Under the declared prior of 0.02 the prior-optimal action is to hold back, at 0.98 * 5 = 4.9; the clairvoyant expected loss is 0, because each state has a zero-loss action available, so EVPI is 4.9."
results:
  model: bayes_decision
  prior_hazard: 0.02
  prior_action: hold_back
  prior_expected_loss: 4.9
  decision_threshold_probability: 0.01
  action_partition:
    - p_from: 0.0
      p_to: 0.01
      action: proceed
    - p_from: 0.01
      p_to: 1.0
      action: hold_back
  clairvoyant_expected_loss: 0.0
  evpi: 4.9
  action_by_probe:
    "0.005": proceed
    "0.02": hold_back
    "0.4": hold_back
  probes:
    "0.005":
      expected_loss:
        proceed: 2.475
        hold_back: 4.975
    "0.02":
      expected_loss:
        proceed: 9.9
        hold_back: 4.9
    "0.4":
      expected_loss:
        proceed: 198.0
        hold_back: 3.0
invariants:
  - expression: "abs(r['decision_threshold_probability'] - 5.0 / (5.0 + 495.0)) < 1e-12"
    description: "p* equals L_conservative / (L_conservative + L_failure)"
  - expression: "r['action_by_probe']['0.4'] == 'hold_back'"
    description: a 40 per cent hazard probability is nowhere near permissive
  - expression: "r['probes']['0.4']['expected_loss']['proceed'] > 60 * r['probes']['0.4']['expected_loss']['hold_back']"
    description: the half-probability rule is wrong by a factor over 60 here
