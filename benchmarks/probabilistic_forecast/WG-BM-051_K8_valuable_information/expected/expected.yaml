# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-051
source: closed_form
derivation: "Under the prior of 0.5 the expected losses are 45 for each evacuation route and 40 for sheltering, so the prior-optimal action is to shelter at 40. P(signal_north) = 0.5 * 0.8 + 0.5 * 0.2 = 0.5, and by Bayes P(omega_north | signal_north) = 0.4 / 0.5 = 0.8. At that belief the expected losses are 72 for the northern road, 18 for the southern road and 40 for sheltering, so the action is to evacuate south at 18. The other branch is the mirror image: P(omega_north | signal_south) = 0.1 / 0.5 = 0.2, and the action is to evacuate north at 18. EVSI = 40 - (0.5 * 18 + 0.5 * 18) = 40 - 18 = 22. Perfect information would allow the correct road in each world at zero loss, so EVPI = 40 - 0 = 40 and the sensor captures 55 per cent of it. The optimal action as a function of P(omega_north) has three segments: the northern road below 4/9, sheltering between 4/9 and 5/9, and the southern road above 5/9 - so there is no single threshold to quote, which is itself worth pinning."
results:
  model: bayes_decision
  prior_hazard: 0.5
  prior_action: shelter
  prior_expected_loss: 40.0
  decision_threshold_probability: null
  action_partition:
    - p_from: 0.0
      p_to: 0.4444444444444444
      action: via_north
    - p_from: 0.4444444444444444
      p_to: 0.5555555555555556
      action: shelter
    - p_from: 0.5555555555555556
      p_to: 1.0
      action: via_south
  clairvoyant_expected_loss: 0.0
  evpi: 40.0
  evsi_statistical_by_observation:
    bearing_sensor: 22.0
  evsi_operational_by_observation:
    bearing_sensor: 22.0
  observations:
    bearing_sensor:
      available_before_deadline: true
      evsi_statistical: 22.0
      evsi_operational: 22.0
      information_gain_bits: 0.2780719051126377
      changes_action: true
      posterior_hazard_by_outcome:
        signal_north: 0.8
        signal_south: 0.2
      action_by_outcome:
        signal_north: via_south
        signal_south: via_north
invariants:
  - expression: "r['observations']['bearing_sensor']['evsi_statistical'] < r['evpi']"
    description: an imperfect observation is worth less than perfect information
  - expression: "len(set(r['observations']['bearing_sensor']['action_by_outcome'].values())) == 2"
    description: the two branches call for different actions
  - expression: "len(r['action_partition']) == 3"
    description: "three actions means three optimality regions, not one threshold"
