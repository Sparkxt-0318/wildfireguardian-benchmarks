# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-049
source: closed_form
derivation: "The decision threshold comes from the loss matrix: evacuating costs 5 if the fire misses and nothing if it reaches, staying costs 20 if it reaches and nothing if it misses, so p* = 5 / (5 + 20) = 0.2. Under the prior of 0.1 the expected loss of staying is 0.1 * 20 = 2 against 0.9 * 5 = 4.5 for evacuating, so the prior-optimal action is to stay. Bayes: P(anomaly) = 0.1 * 0.9 + 0.9 * 0.2 = 0.09 + 0.18 = 0.27, so P(reaches | anomaly) = 0.09 / 0.27 = 1/3 = 0.3333333333333333, which is above p*, and the action becomes evacuate at a posterior expected loss of (2/3) * 5 = 10/3. On the other branch P(no anomaly) = 0.01 + 0.72 = 0.73 and P(reaches | no anomaly) = 0.01 / 0.73 = 0.0136986301369863, well below p*, so the action stays. EVSI = 2 - (0.27 * 10/3 + 0.73 * 0.2739726027397260) = 2 - (0.9 + 0.2) = 0.9. Perfect information would be worth 2 - 0 = 2, since each hypothesis has a zero-loss action. The mutual information of the observation is 0.1448 bits."
results:
  model: bayes_decision
  prior_hazard: 0.1
  prior_action: stay
  prior_expected_loss: 2.0
  decision_threshold_probability: 0.2
  clairvoyant_expected_loss: 0.0
  evpi: 2.0
  observations:
    ridge_anomaly:
      available_before_deadline: true
      evsi_statistical: 0.9
      evsi_operational: 0.9
      information_gain_bits: 0.1448297914506213
      changes_action: true
      realised_outcome: anomaly
      realised_posterior_hazard: 0.33333333333333337
      realised_action: evacuate
      posterior_hazard_by_outcome:
        anomaly: 0.33333333333333337
        no_anomaly: 0.013698630136986302
      action_by_outcome:
        anomaly: evacuate
        no_anomaly: stay
invariants:
  - expression: "abs(r['observations']['ridge_anomaly']['realised_posterior_hazard'] - 1.0 / 3.0) < 1e-12"
    description: the posterior is exactly one third
  - expression: "r['prior_action'] != r['observations']['ridge_anomaly']['realised_action']"
    description: the observation changes the action
  - expression: "r['observations']['ridge_anomaly']['realised_posterior_hazard'] > r['decision_threshold_probability']"
    description: and it changes it by crossing the loss-derived threshold
