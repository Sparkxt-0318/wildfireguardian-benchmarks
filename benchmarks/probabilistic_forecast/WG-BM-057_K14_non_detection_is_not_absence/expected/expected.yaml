# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-057
source: closed_form
derivation: "P(no detection) = 0.2 * 0.3 + 0.8 * 0.95 = 0.06 + 0.76 = 0.82, so P(fire | no detection) = 0.06 / 0.82 = 3/41 = 0.07317073170731707. The negative reading is genuinely informative - it cuts the fire probability from 0.2 to 0.073, a factor of 2.7 - and it does not come close to zero. The loss matrix gives p* = 5 / (5 + 95) = 0.05, and 0.073 is above it, so the conservative action remains correct after the negative reading. The prior action was also to divert, at an expected loss of 0.8 * 5 = 4, and on the positive branch P(fire | detect) = 0.14 / 0.18 = 0.7777777777777777, which is also above p*. Since every branch diverts, EVSI is exactly 0: this detector cannot license standing down, whatever it reports. A system that reads a non-detection as proof of absence sets the posterior to 0, which is below p*, and proceeds into a drainage that is 7.3 per cent likely to be on fire."
results:
  model: bayes_decision
  prior_hazard: 0.2
  prior_action: divert
  prior_expected_loss: 4.0
  decision_threshold_probability: 0.05
  observations:
    detector:
      evsi_statistical: 0.0
      information_gain_bits: 0.27470130018937644
      changes_action: false
      realised_posterior_hazard: 0.07317073170731707
      realised_action: divert
      posterior_hazard_by_outcome:
        detect: 0.7777777777777777
        no_detect: 0.07317073170731707
      action_by_outcome:
        detect: divert
        no_detect: divert
invariants:
  - expression: "r['observations']['detector']['realised_posterior_hazard'] > 0.0"
    description: a non-detection never drives the probability of fire to zero
  - expression: "r['observations']['detector']['realised_posterior_hazard'] < r['prior_hazard']"
    description: "but it is real evidence, and the belief does fall"
  - expression: "r['observations']['detector']['realised_posterior_hazard'] > r['decision_threshold_probability']"
    description: "and it stays above the threshold, so the action does not change"
