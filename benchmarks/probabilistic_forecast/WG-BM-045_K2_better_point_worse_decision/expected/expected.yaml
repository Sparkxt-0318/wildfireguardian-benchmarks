# GENERATED by tools/authoring/author_probabilistic_forecast.py. Edit the script, not this file.
benchmark_id: WG-BM-045
source: closed_form
derivation: "The true world puts the front at 900 m with probability 0.97 and at 700 m with probability 0.03, so its mean is 0.97*900 + 0.03*700 = 894 m and the true probability of the hazard state is 0.03. Since 0.03 < p* = 0.08, the best action under the truth is to use the road, at an expected loss of 0.03*100 = 3. Forecast A is N(910, 400^2): its location error is |910 - 894| = 16 m, the smallest of the two, but Phi((800 - 910)/400) = Phi(-0.275) = 0.3916581191536052 is well above 0.08, so it takes the long way, at an expected loss of 8 in every world. Forecast B is N(940, 50^2): its location error is |940 - 894| = 46 m, nearly three times A's, but Phi((800 - 940)/50) = Phi(-2.8) = 0.002555130330427924 is below 0.08, so it uses the road, at an expected loss of 3 - exactly the best achievable. A's regret is 5 and B's is 0."
results:
  model: predictive_threshold
  decision_threshold_probability: 0.08
  true_mean: 894.0
  true_hazard_probability: 0.03
  best_action_under_truth: use_road
  best_expected_loss_under_truth: 3.0
  hazard_probability_by_forecast:
    forecast_a: 0.39165811915360516
    forecast_b: 0.002555130330427924
  action_by_forecast:
    forecast_a: long_way
    forecast_b: use_road
  location_error_by_forecast:
    forecast_a: 16.0
    forecast_b: 46.0
  expected_loss_under_truth_by_forecast:
    forecast_a: 8.0
    forecast_b: 3.0
  regret_by_forecast:
    forecast_a: 5.0
    forecast_b: 0.0
invariants:
  - expression: "r['location_error_by_forecast']['forecast_a'] < r['location_error_by_forecast']['forecast_b']"
    description: forecast A has the better deterministic skill
  - expression: "r['expected_loss_under_truth_by_forecast']['forecast_a'] > r['expected_loss_under_truth_by_forecast']['forecast_b']"
    description: and the worse decision outcome
  - expression: "r['regret_by_forecast']['forecast_b'] == 0.0"
    description: the less accurate forecast attains the best achievable expected loss
