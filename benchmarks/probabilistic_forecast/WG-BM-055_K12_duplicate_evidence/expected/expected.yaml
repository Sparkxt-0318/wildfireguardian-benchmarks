# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-055
source: closed_form
derivation: "There is one measurement. Its likelihood is 0.8 for a positive report given danger and 0.2 given safety, so a clear report gives P(dangerous | clear) = 0.5 * 0.2 / (0.5 * 0.2 + 0.5 * 0.8) = 0.2, and the log-likelihood ratio is log2(0.2 / 0.8) = -2 bits exactly. A system that treats the two records as two independent observations multiplies 0.2 * 0.2 = 0.04 against 0.8 * 0.8 = 0.64, obtaining 0.04 / 0.68 = 1/17 = 0.058823529411764705 and a log-likelihood ratio of -4 bits: exactly twice the evidence, from exactly no extra data. With p* = 0.1 the correct posterior 0.2 is above the threshold and the action is to divert; the doubled posterior 0.0588 is below it and the action becomes proceed. Deduplication is by measurement id, not by record id: the two records have different record ids and are the same measurement, which is precisely the case an ingestion pipeline keyed on record id will get wrong."
results:
  model: bayes_decision
  prior_hazard: 0.5
  prior_action: divert
  prior_expected_loss: 5.0
  decision_threshold_probability: 0.1
  observations:
    duplicated_report:
      record_count: 2
      independent_measurements: 1
      contains_duplicates: true
      evsi_statistical: 0.0
      information_gain_bits: 0.2780719051126377
      realised_posterior_hazard: 0.2
      realised_action: divert
      realised_posterior_under_independence: 0.058823529411764705
      realised_action_under_independence: proceed
      realised_log_likelihood_ratio_bits: -2.0
      realised_log_likelihood_ratio_bits_under_independence: -4.0
invariants:
  - expression: "r['observations']['duplicated_report']['independent_measurements'] == 1"
    description: "two records, one measurement"
  - expression: "abs(r['observations']['duplicated_report']['realised_log_likelihood_ratio_bits_under_independence'] - 2 * r['observations']['duplicated_report']['realised_log_likelihood_ratio_bits']) < 1e-12"
    description: double counting doubles the evidence exactly
  - expression: "r['observations']['duplicated_report']['realised_action'] != r['observations']['duplicated_report']['realised_action_under_independence']"
    description: and the doubling changes the action
