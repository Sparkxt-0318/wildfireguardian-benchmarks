# GENERATED by tools/authoring/author_bayesian_inference.py. Edit the script, not this file.
benchmark_id: WG-BM-052
source: closed_form
derivation: "The decision problem is identical to WG-BM-051, so the statistical quantities are unchanged: the prior action is to shelter at 40, each branch gives 18, and EVSI is 22 with an EVPI of 40. Only the timing differs. The sensor acquires at minute 5 and its product is available at minute 12, five minutes after the decision must be taken at minute 10, so no policy can condition on it. The operational value of the observation is therefore 0, and the action remains the prior-optimal shelter. The gap between 22 and 0 is entirely a property of the observing system's latency, not of its accuracy: the same sensor with the same likelihoods is worth 22 in WG-BM-051 and nothing here."
results:
  model: bayes_decision
  prior_action: shelter
  prior_expected_loss: 40.0
  evpi: 40.0
  observations:
    bearing_sensor:
      acquisition_time_min: 5.0
      availability_time_min: 12.0
      available_before_deadline: false
      evsi_statistical: 22.0
      evsi_operational: 0.0
      information_gain_bits: 0.2780719051126377
  evsi_statistical_by_observation:
    bearing_sensor: 22.0
  evsi_operational_by_observation:
    bearing_sensor: 0.0
invariants:
  - expression: "r['observations']['bearing_sensor']['evsi_statistical'] > 0"
    description: the information exists
  - expression: "r['observations']['bearing_sensor']['evsi_operational'] == 0.0"
    description: and no decision can use it
  - expression: "r['observations']['bearing_sensor']['available_before_deadline'] is False"
    description: "because it arrives after the deadline, which is reported explicitly"
