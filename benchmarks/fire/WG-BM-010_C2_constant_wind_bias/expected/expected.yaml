# GENERATED by tools/authoring/author_fire.py. Edit the script, not this file.
benchmark_id: WG-BM-010
source: closed_form
derivation: "The front at time t is the ellipse centred at (c t, 0) with semi-axes a t along the wind and b t across it, where a = (head + back)/2 = 12, c = (head - back)/2 = 8 and b = 6. Directly downwind the front advances at a + c = 20 m/min, so 200 m arrives at 10 min. Directly upwind it backs at a - c = 4 m/min, so 200 m upwind arrives at 50 min. Across the wind, setting p = 0 in the front equation gives t = a q / (b sqrt(a^2 - c^2)) = 12 * 200 / (6 * sqrt(80)) = 20 sqrt(5) = 44.7213595499958 min. Two further probes are read directly off the t = 10 ellipse, which is centred at (80, 0) with semi-axes 120 and 60: the point (80, 60) is its northern extremity and (-40, 0) its western extremity, so both arrive at exactly 10 min. Note that (-40, 0) also follows from the backing rate: 40 / 4 = 10."
results:
  model: elliptical_wind
  arrival_min:
    p_downwind: 10.0
    p_upwind: 50.0
    p_crosswind: 44.72135954999579
    p_front_flank: 10.0
    p_front_back: 10.0
  arrival_order: [p_downwind, p_front_back, p_front_flank, p_crosswind, p_upwind]
  first_reached: p_downwind
  last_reached: p_upwind
invariants:
  - expression: "abs(r['arrival_min']['p_upwind'] / r['arrival_min']['p_downwind'] - 5.0) < 1e-9"
    description: "head-to-back anisotropy ratio is exactly 20/4 = 5"
  - expression: "r['arrival_min']['p_downwind'] < r['arrival_min']['p_crosswind'] < r['arrival_min']['p_upwind']"
    description: "head arrives first, flank second, back last"
