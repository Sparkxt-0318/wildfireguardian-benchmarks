# GENERATED by tools/authoring/author_graph.py. Edit the script, not this file.
benchmark_id: WG-BM-005
source: exhaustive_enumeration
derivation: "The graph is a path of three nodes. The only simple walk from village to exit is village-a-exit, so the egress route count is 1 and the travel time is 2 + 3 = 5 min. Deleting 'a' leaves {village} and {exit} in separate components, so 'a' is an articulation point; deleting either terminal leaves a connected two-node graph, so neither is. Both edges are bridges, since a path graph has no cycles. The smallest set of non-terminal nodes separating village from exit is {a}, of size 1, so by Menger's theorem there is exactly 1 internally node-disjoint egress path."
results:
  node_count: 3
  edge_count: 2
  components: 1
  articulation_points: [a]
  bridges: [e_village_a, e_a_exit]
  reachable_destinations: [exit]
  unreachable_destinations: []
  selected_destination: exit
  min_internal_node_cut: 1
  min_internal_node_cut_set: [a]
  single_point_of_failure: true
  node_disjoint_egress_paths: 1
  destinations:
    exit:
      reachable: true
      simple_path_count: 1
      min_travel_time_min: 5.0
      euclidean_distance_m: 3000.0
invariants:
  - expression: "r['destinations']['exit']['paths'] == [['e_village_a', 'e_a_exit']]"
    description: the single egress route is enumerated explicitly
