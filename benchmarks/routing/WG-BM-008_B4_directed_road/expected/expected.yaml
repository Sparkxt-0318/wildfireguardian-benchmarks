# GENERATED by tools/authoring/author_graph.py. Edit the script, not this file.
benchmark_id: WG-BM-008
source: exhaustive_enumeration
derivation: "Edges e_ab and e_bc are one-way in the stated direction; e_ad is two-way. Following directions only: from a you can reach b, c (via b) and d, so a->c is true with the single route e_ab+e_bc taking 3 + 4 = 7 min. From c there is no outgoing edge at all, so c reaches nothing: c->a and c->b are both false. From d the two-way road reaches a, and from a the corridor reaches c, so d->c is true. Reachability is therefore asymmetric, which is the whole content of the case."
results:
  node_count: 4
  edge_count: 3
  reachable_destinations: [c, d]
  unreachable_destinations: []
  reachability_checks:
    "a->c": true
    "c->a": false
    "d->c": true
    "c->b": false
  destinations:
    c:
      reachable: true
      simple_path_count: 1
      min_travel_time_min: 7.0
    d:
      reachable: true
      simple_path_count: 1
      min_travel_time_min: 2.0
invariants:
  - expression: "r['reachability_checks']['a->c'] and not r['reachability_checks']['c->a']"
    description: reachability is asymmetric across the one-way corridor
