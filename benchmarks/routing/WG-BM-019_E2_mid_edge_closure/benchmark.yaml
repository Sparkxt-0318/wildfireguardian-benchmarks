# GENERATED by tools/authoring/author_routing.py. Edit the script, not this file.
benchmark_id: WG-BM-019
label: E2
title: "Mid-edge closure: the answer depends on a semantic choice that must be declared"
category: routing
difficulty: adversarial
purpose: "A traveller enters a 10-minute edge at t = 0 and the hazard reaches it at t = 5. There is no convention-free answer, so the convention is part of the benchmark."
solver: routing.time_dependent_enumeration
assumptions:
  edge_semantics: WG-SEM-1 interval safety
  declared_answer_under: interval safety
  alternative_reported: entry-time-only
  no_turning_back: true
  waiting: false
expected_behavior:
  any_feasible: false
  feasible_under_entry_time_semantics: true
tolerance:
  default: 1.0e-09
exactness: CLOSED_FORM
detects: [mid_edge_hazard, undeclared_traversal_semantics]
mutations_expected_to_fail: [edge_entry_time_only]
hand_checkable: true
notes: "This benchmark asserts a convention, not a law of nature. Its value is that an implementation cannot pass it without stating which convention it uses."
inputs:
  network: inputs/network.yaml
  query: inputs/query.yaml
