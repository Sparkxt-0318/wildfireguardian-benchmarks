# GENERATED by tools/authoring/author_terrain.py. Edit the script, not this file.
benchmark_id: WG-BM-003
source: hand_derivation
derivation: "Elevation depends only on the column: z = 100 - 6|col - 3|, so dz/dy = 0 everywhere and the problem is one dimensional. On the west flank (col 2) the east neighbours are at 100 and the west neighbours at 88, giving dz/dx = (4*100 - 4*88) / (8*30) = 48/240 = +0.2; the surface rises to the east, so it descends to the west and the aspect is 270 deg. On the east flank (col 4) the same computation gives dz/dx = (4*88 - 4*100)/240 = -0.2 and an aspect of 90 deg. slope = atan(0.2) = 11.3099... degrees on both flanks. At the crest (col 3) the neighbours on both sides are at 94, so the centred difference is exactly zero and the cell reports slope 0 with undefined aspect. That is a known artefact of centred differencing at a ridge line, not flat ground: the interior rows are 1..3, so exactly 3 cells show it."
results:
  rows: 5
  cols: 7
  interior_cells: 15
  interior_defined: 15
  interior_undefined: 0
  flat_interior_cells: 3
  max_slope_deg: 11.309932474020215
  min_slope_deg: 0.0
  probes:
    crest:
      slope_deg: 0.0
      aspect_deg: null
      status: ok
    west_flank:
      slope_deg: 11.309932474020215
      aspect_deg: 270.0
      status: ok
    east_flank:
      slope_deg: 11.309932474020215
      aspect_deg: 90.0
      status: ok
invariants:
  - expression: "all(r['aspect_deg_grid'][row][3] is None for row in (1, 2, 3))"
    description: every interior crest cell has undefined aspect
  - expression: "all(r['aspect_deg_grid'][row][col] == 270.0 for row in (1, 2, 3) for col in (1, 2))"
    description: the whole west flank faces west
  - expression: "all(r['aspect_deg_grid'][row][col] == 90.0 for row in (1, 2, 3) for col in (4, 5))"
    description: the whole east flank faces east
