# GENERATED by tools/authoring/author_terrain.py. Edit the script, not this file.
benchmark_id: WG-BM-001
source: hand_derivation
derivation: "Every Horn finite difference on a constant field is a difference of equal sums, so dz/dx = dz/dy = 0 exactly and slope = atan(0) = 0. The aspect of a zero gradient is not defined: atan2(0, 0) is a convention, not a measurement, so the expected value is null. The 5x5 grid has 3x3 = 9 interior cells; border cells have no complete neighbourhood and are reported as null with status 'border'."
results:
  rows: 5
  cols: 5
  interior_cells: 9
  interior_defined: 9
  interior_undefined: 0
  flat_interior_cells: 9
  max_slope_deg: 0.0
  min_slope_deg: 0.0
  probes:
    centre:
      slope_deg: 0.0
      aspect_deg: null
      status: ok
    off_centre:
      slope_deg: 0.0
      aspect_deg: null
      status: ok
invariants:
  - expression: "all(v == 0.0 for row in r['slope_deg_grid'] for v in row if v is not None)"
    description: no cell anywhere on the grid reports a non-zero slope
  - expression: "all(v is None for row in r['aspect_deg_grid'] for v in row)"
    description: aspect is undefined everywhere on a flat plane
