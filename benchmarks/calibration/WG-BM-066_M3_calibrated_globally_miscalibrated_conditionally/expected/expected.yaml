# GENERATED by tools/authoring/author_calibration.py. Edit the script, not this file.
benchmark_id: WG-BM-066
source: closed_form
derivation: "Pooled over the 800 cases there is a single forecast probability of 0.5 and 400 events, an observed frequency of exactly 0.5. The aggregate expected calibration error and reliability term are therefore both zero and the forecast is, in aggregate, perfectly calibrated. Split by stratum it is wrong everywhere: 0.5 stated against 0.2 observed in calm conditions and 0.5 against 0.8 in gusty ones, each an error of 0.3 in opposite directions. Since the strata are equally sized, the stratified expected calibration error is 0.5 * 0.3 + 0.5 * 0.3 = 0.3 and the stratified reliability term is 0.5 * 0.09 + 0.5 * 0.09 = 0.09. The two errors cancel exactly, which is why the aggregate is clean. The loss matrix gives p* = 30 / (30 + 70) = 0.3. The stated 0.5 is above it, so the forecast says divert in both strata. In gusty conditions that is right, since 0.8 is also above the threshold. In calm conditions it is wrong: the true rate of 0.2 is below the threshold, proceeding costs 0.2 * 70 = 14 per case and diverting costs 0.8 * 30 = 24, an excess of 10 on each of 400 cases - 4000 in total, or 5 per case over the whole sample."
results:
  aggregate_calibration_error: 0.0
  perfectly_calibrated: true
  brier_score: 0.25
  reliability: 0.0
  resolution: 0.0
  uncertainty: 0.25
  base_rate: 0.5
  stratified_calibration_error: 0.3
  aggregate_hides_stratum_failure: true
  stratified:
    expected_calibration_error: 0.3
    reliability: 0.09
    resolution: 0.09
    uncertainty: 0.25
    brier_score: 0.25
    base_rate: 0.5
  decision:
    decision_threshold_probability: 0.3
    groups_with_different_action: ["calm|p=0.5"]
    total_excess_expected_loss: 4000.0
    excess_expected_loss_per_case: 5.0
invariants:
  - expression: "r['aggregate_calibration_error'] == 0.0 and r['stratified_calibration_error'] > 0.2"
    description: "flawless in aggregate, badly wrong conditionally"
  - expression: "r['aggregate_hides_stratum_failure'] is True"
    description: and the report says so rather than leaving it to be noticed
  - expression: "r['decision']['excess_expected_loss_per_case'] > 0"
    description: the hidden miscalibration has a decision cost
