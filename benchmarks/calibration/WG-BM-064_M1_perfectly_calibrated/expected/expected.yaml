# GENERATED by tools/authoring/author_calibration.py. Edit the script, not this file.
benchmark_id: WG-BM-064
source: closed_form
derivation: "The five groups are 100 cases at p = 0 with 0 events, 200 at 0.2 with 40, 100 at 0.5 with 50, 300 at 0.8 with 240 and 100 at 1.0 with 100. Every observed frequency equals its forecast probability, so every calibration error is zero and both the expected calibration error and the reliability term are zero. The base rate is 430/800 = 0.5375, so the uncertainty term is 0.5375 * 0.4625 = 0.24859375. The resolution term is the weighted sum of (observed - base rate) squared: 0.125*0.28890625 + 0.25*0.11390625 + 0.125*0.00140625 + 0.375*0.06890625 + 0.125*0.21390625 = 0.11734375. By Murphy's decomposition the Brier score is 0 - 0.11734375 + 0.24859375 = 0.13125, and computing it directly group by group gives the same 0.13125, so the residual is zero. The loss matrix puts the decision threshold at p* = 10/(10+90) = 0.1. In every group the action taken from the forecast probability is the same as the action taken from the observed frequency, so the excess expected loss from miscalibration is zero - which is what perfect calibration buys."
results:
  aggregate_calibration_error: 0.0
  reliability: 0.0
  resolution: 0.11734375
  uncertainty: 0.24859375
  brier_score: 0.13125
  murphy_residual: 0.0
  base_rate: 0.5375
  perfectly_calibrated: true
  decision:
    decision_threshold_probability: 0.1
    groups_with_different_action: []
    total_excess_expected_loss: 0.0
    excess_expected_loss_per_case: 0.0
invariants:
  - expression: "abs(r['murphy_residual']) < 1e-12"
    description: "Brier = reliability - resolution + uncertainty, exactly"
  - expression: "all(abs(g['calibration_error']) < 1e-12 for g in r['aggregate']['groups'].values())"
    description: every group is individually calibrated
  - expression: "r['decision']['total_excess_expected_loss'] == 0.0"
    description: perfect calibration costs nothing in decisions
