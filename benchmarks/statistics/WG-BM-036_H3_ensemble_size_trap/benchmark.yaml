# GENERATED by tools/authoring/author_scenario.py. Edit the script, not this file.
benchmark_id: WG-BM-036
label: H3
title: "Adding two 0.1% scenarios moves expected loss by 0.5 and sup-regret by 8"
category: scenario_uncertainty
difficulty: adversarial
purpose: "Sup-regret is a maximum over the scenario set, so it grows mechanically as scenarios are added, and the minimax-regret recommendation flips on 0.2% of probability mass. CVaR does not."
solver: scenario.ensemble_analysis
assumptions:
  extreme_scenario_probability: 0.001
  base_scenarios_rescaled: true
  cvar_alpha: 0.9
expected_behavior:
  ensemble_comparison:
    sup_regret_change: 8.0
    minimax_action_changed: true
tolerance:
  default: 1.0e-09
exactness: exact_analytic
detects: [sup_regret_scales_with_ensemble_size, worst_case_criterion_instability]
mutations_expected_to_fail: []
hand_checkable: true
notes: "No mutation is needed here: the benchmark's content is a property of the decision criteria themselves, which is exactly why it must be pinned."
inputs:
  scenario_analysis: inputs/scenario_analysis.yaml
