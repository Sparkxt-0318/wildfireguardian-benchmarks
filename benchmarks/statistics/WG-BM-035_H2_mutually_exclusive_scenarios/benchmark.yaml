# GENERATED by tools/authoring/author_scenario.py. Edit the script, not this file.
benchmark_id: WG-BM-035
label: H2
title: "Mutually exclusive scenarios: averaging the inputs says both routes are fine"
category: scenario_uncertainty
difficulty: adversarial
purpose: "Route A is right in scenario 1 and fatal in scenario 2, and vice versa. Averaging the fire fields produces a world in which neither route is blocked and the expected loss is zero. Averaging the losses gives 50."
solver: scenario.ensemble_analysis
assumptions:
  scenarios_mutually_exclusive: true
  averaged_world_losses_are_given: true
  cvar_alpha: 0.9
expected_behavior:
  averaged_world_best_action: route_a
  ensembles:
    base:
      best_action_by_expected_loss: hold
tolerance:
  default: 1.0e-09
exactness: exact_analytic
detects: [average_of_inputs_fallacy, scenario_blending]
mutations_expected_to_fail: [average_scenario_inputs]
hand_checkable: true
inputs:
  scenario_analysis: inputs/scenario_analysis.yaml
