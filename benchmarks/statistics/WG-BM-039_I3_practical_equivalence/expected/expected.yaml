# GENERATED by tools/authoring/author_statistics.py. Edit the script, not this file.
benchmark_id: WG-BM-039
source: closed_form
derivation: "The 2000 paired differences are 1000 values of +1.3 and 1000 of -0.7, so the sample mean is exactly 0.3 and every deviation from it is exactly 1.0 in magnitude. The sample standard deviation is therefore sqrt(2000/1999) = 1.0002501 and the standard error is 1.0002501/sqrt(2000) = 0.0223717. The 95 per cent normal confidence interval is 0.3 +/- 1.959964 * 0.0223717 = [0.256152, 0.343848]. Significance: the interval excludes zero by a wide margin, and |0.3| is 13.4 standard errors from zero, so the difference is significant at any conventional level. Practical equivalence: the whole interval lies inside the pre-agreed margin of +/- 1.0 minute, so the two policies are practically equivalent. Both statements are true simultaneously."
results:
  analysis: equivalence
  n: 2000
  mean_difference: 0.3
  sd_difference: 1.0002500937890797
  standard_error: 0.022366272042129223
  ci_lower: 0.25616291232900157
  ci_upper: 0.3438370876709984
  practical_margin: 1.0
  practically_equivalent: true
  statistically_significant: true
  significant_but_not_meaningful: true
  conclusion: equivalent
invariants:
  - expression: "r['ci_lower'] > -r['practical_margin'] and r['ci_upper'] < r['practical_margin']"
    description: the whole confidence interval lies inside the practical margin
  - expression: "r['ci_lower'] > 0"
    description: "the interval also excludes zero, so the difference is significant"
  - expression: "abs(r['mean_difference'] / r['standard_error']) > 10"
    description: the effect is more than ten standard errors from zero
