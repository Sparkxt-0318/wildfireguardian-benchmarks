# GENERATED by tools/authoring/author_risk.py. Edit the script, not this file.
benchmark_id: WG-BM-060
source: exhaustive_enumeration
derivation: "E[policy_a] = 0.85 * 0 + 0.10 * 20 + 0.05 * 300 = 0 + 2 + 15 = 17 and E[policy_b] = 0.85 * 18 + 0.10 * 18 + 0.05 * 40 = 15.3 + 1.8 + 2 = 19.1, so A has the better mean by 2.1. CVaR at 0.9 averages the worst tenth of the probability mass. For A that tenth is the whole 0.05 atom at 300 plus half of the 0.10 atom at 20: (0.05 * 300 + 0.05 * 20) / 0.1 = (15 + 1) / 0.1 = 160. For B the worst tenth is the 0.05 atom at 40 plus half the mass at 18: (0.05 * 40 + 0.05 * 18) / 0.1 = (2 + 0.9) / 0.1 = 29. A's conditional tail loss is five and a half times B's. The best action in each scenario is A in the routine case, B in the other two, so A's maximum regret is 260 and B's is 18, and B is also the minimax-regret choice. The clairvoyant expected loss is 0.85 * 0 + 0.10 * 18 + 0.05 * 40 = 3.8, so EVPI is 17 - 3.8 = 13.2. With the objective declared as CVaR the recommendation is policy_b. Reporting the expected-loss winner instead silently substitutes one risk attitude for another."
results:
  objective: cvar
  cvar_alpha: 0.9
  expected_loss:
    policy_a: 17.0
    policy_b: 19.1
  cvar:
    policy_a: 160.0
    policy_b: 29.0
  worst_case_loss:
    policy_a: 300.0
    policy_b: 40.0
  max_regret:
    policy_a: 260.0
    policy_b: 18.0
  best_by_expected_loss: policy_a
  best_by_cvar: policy_b
  best_by_worst_case: policy_b
  rankings_conflict: true
  recommended_action: policy_b
  winner_declared: true
  clairvoyant_expected_loss: 3.8
  evpi: 13.2
invariants:
  - expression: "r['expected_loss']['policy_a'] < r['expected_loss']['policy_b']"
    description: A has the better mean
  - expression: "r['cvar']['policy_a'] > 5 * r['cvar']['policy_b']"
    description: and a conditional tail loss over five times worse
  - expression: "r['recommended_action'] == r['best_by_cvar']"
    description: the recommendation follows the declared objective
