# GENERATED by tools/authoring/author_risk.py. Edit the script, not this file.
benchmark_id: WG-BM-063
source: exhaustive_enumeration
derivation: "E[stage_at_junction] = 0.4*4 + 0.3*4 + 0.2*5 + 0.1*5 = 1.6 + 1.2 + 1.0 + 0.5 = 4.3; E[stage_north] = 2.4 + 2.7 + 2.4 + 2.0 = 9.5; E[stage_south] = 8.0 + 3.6 + 1.8 + 0.6 = 14.0. Staging at the junction is also the minimum in each scenario separately - 4, 4, 5, 5 against 6, 9, 9 and 6 for the best alternative in each - so the clairvoyant expected loss is 4.3 and EVPI is exactly 0. The forecast is poor: it spreads 0.4 / 0.3 / 0.2 / 0.1 across four behaviours, its declared skill score is 0.25, and the largest scenario weight is 0.4, far below the resolution threshold of 0.8, so the state is unresolved. The decision is not: the runner-up is 5.2 behind, and the smallest single-cell perturbation that would flip it is 5.2 / 0.4 = 13.0, thirteen times the declared tolerance. The junction's worst case is 5, inside the acceptable loss of 8, so it is robust as well as optimal."
results:
  objective: expected_loss
  expected_loss:
    stage_at_junction: 4.3
    stage_north: 9.5
    stage_south: 14.0
  worst_case_loss:
    stage_at_junction: 5.0
    stage_north: 20.0
    stage_south: 20.0
  cvar:
    stage_at_junction: 5.0
    stage_north: 20.0
    stage_south: 20.0
  recommended_action: stage_at_junction
  clairvoyant_expected_loss: 4.3
  evpi: 0.0
  information_would_change_action: false
  action_optimal_in_every_scenario: [stage_at_junction]
  robust_actions: [stage_at_junction]
  state_certainty: 0.4
  state_resolved: false
  decision_margin: 5.2
  min_perturbation_to_flip: 13.0
  decision_stable: true
  decision_resolved: true
  forecast_skill_score: 0.25
invariants:
  - expression: "r['forecast_skill_score'] < 0.5 and r['decision_resolved'] is True"
    description: "poor skill, resolved decision"
  - expression: "r['evpi'] == 0.0"
    description: improving the forecast could not improve the decision
  - expression: "r['min_perturbation_to_flip'] > 10 * r['perturbation_tolerance']"
    description: the recommendation is stable by an order of magnitude
